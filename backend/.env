# Server
NODE_ENV=development
PORT=5000
CORS_ORIGIN=http://localhost:5173

# MongoDB
MONGO_URI=mongodb+srv://vhari2812_db_user:hariprasath2812@cluster0.u5pftns.mongodb.net/?appName=Cluster0

# JWT
JWT_ACCESS_SECRET=change_me_to_a_long_random_secret

# Stripe (set ENABLE_STRIPE=false to boot without payments)
ENABLE_STRIPE=true
STRIPE_SECRET_KEY=sk_test_****************
STRIPE_WEBHOOK_SECRET=whsec_****************
STRIPE_CURRENCY=usd

# Email (optional; bookings still work if SMTP is not configured)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
EMAIL_FROM=your_email@gmail.com

# Optional: register this email as the initial admin user
SEED_ADMIN_EMAIL=admin@example.com

